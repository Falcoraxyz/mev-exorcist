# Production Environment Variables Template
# Copy these to your deployment platform's environment variable settings

# REQUIRED: Alchemy WebSocket URL for Ethereum Sepolia testnet
# Get your API key from: https://dashboard.alchemy.com/
ALCHEMY_WSS_URL=wss://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY_HERE

# REQUIRED: Uniswap V3 Router address on Sepolia testnet
# This is the official Uniswap V3 Router address - do not change
UNISWAP_V3_ROUTER=0xE592427A0AEce92De3Edee1F18E0157C05861564

# OPTIONAL: Server port (usually auto-assigned by platform)
# Railway/Render will automatically assign a port
# Only set this if you need a specific port
# PORT=3001

# OPTIONAL: Risk threshold in ETH for high-risk classification
# Transactions with value >= this threshold are marked as HIGH risk
RISK_THRESHOLD_ETH=0.1

# REQUIRED: Node environment
# Always set to 'production' for deployed environments
NODE_ENV=production

# DEPLOYMENT PLATFORM NOTES:
# 
# Railway:
#   - PORT is automatically set by Railway
#   - Add all other variables in the "Variables" tab
#   - railway.json is already configured
#
# Render:
#   - PORT is automatically set by Render
#   - Add all other variables in the "Environment" section
#   - render.yaml is already configured
#
# Docker:
#   - Pass all variables using -e flags or docker-compose
#   - Example: docker run -e ALCHEMY_WSS_URL="wss://..." -e NODE_ENV="production" ...
